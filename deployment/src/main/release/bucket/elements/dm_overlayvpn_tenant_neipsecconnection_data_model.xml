<?xml version="1.0" encoding="utf-8"?>
<inventory xmlns="http://www.w3school.com.cn" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.w3school.com.cn inv-datamodel-1.0.xsd">
    <datamodel name="inv_overlayvpn_tenant_neipsecconnection">
        <index name="idx_overlayvpn_tenant_neipsecconnection_uniqueid" value="uuid" isunique="true" />
        <filter name="default"
            value="uuid in (:uuid) and adminStatus in (:adminStatus) and tenantId in (:tenantId) and name in (:name) and description in (:description) and source in (:source) and actionState in (:actionState) and operStatus in (:operStatus) and createModelTime in (:createModelTime) and controllerId in (:controllerId) and externalId in (:externalId) and externalIpSecId in (:externalIpSecId) and connectionServiceId in (:connectionServiceId) and neId in (:neId) and soureIfName in (:soureIfName) and peerNeId in (:peerNeId) and sourceAddress in (:sourceAddress) and topoRole in (:topoRole) and peerAddress in (:peerAddress) and authMode in (:authMode) and psk in (:psk) and ikePolicyId in (:ikePolicyId) and ipsecPolicyId in (:ipsecPolicyId)" />
    </datamodel>
</inventory>
